# 이 파일을 .env 로 복사한 뒤 키를 채운다. .env 는 git 추적에서 제외된다.

# 임베딩 — 측정 결과 text-embedding-3-large 로 확정 (3072차원)
OPENAI_API_KEY=
EMBEDDING_MODEL=text-embedding-3-large

# 4주차 LangGraph 멀티에이전트용
ANTHROPIC_API_KEY=
